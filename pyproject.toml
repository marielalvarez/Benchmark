[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qcnn-benchmark"
version = "0.1.0"
description = "Benchmark multifactorial de QCNNs (Hur, Wei, Gong, Cong) vs. CNNs análogas"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
